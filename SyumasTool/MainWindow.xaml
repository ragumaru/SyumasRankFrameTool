<Window x:Class="SyumasTool.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SyumasTool"
        mc:Ignorable="d"
        Title="週マスランキング フレーム作成ツール" Height="190" Width="500">
    <Grid Margin="10,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Label>ランキングExcelファイル名(.xls,.xlsx)</Label>
        <TextBox Name="FileTextBox" Margin="0,0,5,0" Padding="5" Grid.Row="1"></TextBox>
        <Button Click="FileBrowseButton_Click" Padding="10,0" Grid.Row="1" Grid.Column="1">...</Button>
        <Button Click="ExecButton_Click" HorizontalAlignment="Right" VerticalAlignment="Bottom" Padding="20,8" Grid.Row="2" Grid.ColumnSpan="2">実行</Button>

    </Grid>
</Window>
